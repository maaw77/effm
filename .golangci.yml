run:
  timeout: 15m

linters:
  disable-all: true
  enable:
    - gofmt
    - govet
    - staticcheck
    - unused
    - ineffassign
    - misspell

issues:
  exclude-dirs:
    - docs
    - migrations
  exclude-use-default: true
  max-issues-per-linter: 0
  max-same-issues: 0