run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  disable-all: true
  enable:
    - gofmt
    - gosec
    - govet
    - staticcheck
    - unused
    - ineffassign
    - misspell

issues:
  exclude-use-default:  true
  max-issues-per-linter: 0
  max-same-issues: 0